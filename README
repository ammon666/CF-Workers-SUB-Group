# # 🚀 Cloudflare Workers 多分组订阅管理服务
> 基于 Cloudflare Workers + KV 持久化存储构建的轻量化节点订阅系统，权限严格隔离，安全可控

## ✨ 核心亮点
- 🔐 **双层权限隔离**
  - 管理后台：独立环境变量密钥 `ADMIN_TOKEN`，仅路径访问
  - 订阅通道：每组组合独立 24 位随机 Token，订阅凭证**无法进入管理面板**
- 📦 **多分组自由管理**：灵活创建/编辑/删除节点分组，独立维护各组节点清单
- 🧩 **自定义组合订阅**：勾选任意数量分组一键生成专属订阅链接
- 🏷️ **节点名自动加分组前缀**：订阅下发时自动为每个分组内的节点名称添加 `[分组名]-` 前缀，在客户端中可直观识别节点归属；原始节点文本保持不变，不会修改存储
- 📝 **自定义订阅名称**：可选填写订阅备注名，留空自动拼接绑定分组名称
- 🗑️ **订阅快速作废**：后台一键删除订阅，对应链接立即返回 403 拒绝访问
- 🔗 **外部订阅聚合**：支持导入多条外部订阅地址，与本地节点合并输出
- 📡 **标准订阅输出**：节点聚合后 Base64 编码，兼容各类订阅客户端

---

## 📋 部署前置条件
1. 拥有可用 Cloudflare 账号，可使用 Workers & Pages 服务
2. 提前创建一个 **Workers KV 命名空间**（用于持久化分组、订阅绑定数据）

## ⚙️ 环境变量配置清单
| 环境变量名称 | 是否必填 | 参数说明 |
| :--- | :---: | :--- |
| `ADMIN_TOKEN` | ✅ **必填** | 高强度随机字符串，管理后台访问密钥，严禁泄露 |
| `SUBAPI` | ❌ 可选 | 外部订阅地址，多地址使用 `|` 符号分隔，自动合并进订阅内容 |
| `SUBNAME` | ❌ 可选 | 管理页面自定义标题文本 |
| `SUBUPDATE_TIME` | ❌ 可选 | HTTP 响应头 `Profile-Update-Interval`（单位：秒），默认值：`6` |
| `URL302` | ❌ 可选 | 非法访问时 302 跳转目标地址 |
| `URL` | ❌ 可选 | 请求反向代理目标地址 |

## 🛠️ 完整分步部署教程
### Step 1：创建 KV 命名空间
1. Cloudflare 控制台 → `Workers & Pages` → `KV`
2. 点击「创建命名空间」，自定义名称并记录，后续绑定至 Worker

### Step 2：新建 Worker 应用
1. `Workers & Pages` → 创建应用程序 → 创建 Worker
2. 自定义 Worker 名称，点击「部署」生成基础实例

### Step 3：替换 Worker 代码
1. 部署完成后，点击「快速编辑」进入代码编辑器
2. 清空编辑器内默认代码，粘贴完整项目 JS 代码，点击保存

### Step 4：绑定 KV 存储命名空间
1. 顶部切换至「设置」标签 → 「变量」分类
2. 找到 `KV 命名空间绑定` 区域：
   - 绑定变量名**固定填写**：`KV_STORE_KEY`
   - 下拉选择上一步创建的 KV 命名空间，保存绑定

### Step 5：配置环境变量
同一「设置-变量」页面，在「环境变量」板块添加表格内所需变量，**必须配置 ADMIN_TOKEN**。

### Step 6：生效全部配置
返回代码编辑页面，点击「保存并部署」，所有配置将正式生效。

---

## 📖 使用操作指南
### 1. 进入管理后台
访问地址格式：https:// 你的 worker 域名？token=24 位随机订阅 Token
- 无 Token / Token 无效 / 已删除订阅 Token：返回 `403 Access Denied`
- 有效订阅 Token：聚合所有绑定分组节点 + 外部订阅，输出标准 Base64 订阅内容

### 2. 节点名称前缀说明
- 在管理面板 textarea 中你粘贴/编辑的是**原始节点文本**，保存到 KV 的也是原始节点，便于复用与编辑
- 订阅下发时会自动给每个分组内的节点名称附加 `[分组名]-` 前缀，便于在客户端中一眼看出该节点归属哪个分组
- 例如：`#vl-reality-instance-20260419-1809` → `#[GroupA]-vl-reality-instance-20260419-1809`
- 该前缀**仅在订阅响应中生效**，不会回写到管理面板中的原始节点文本，修改分组名后下次订阅将自动使用新前缀

---

## ⚠️ 重要安全提示
1. `ADMIN_TOKEN` 使用复杂、长随机字符串，禁止简单数字/常用词汇，切勿对外分享
2. 订阅独立随机 Token 仅能获取节点订阅内容，**不存在任何访问管理后台的权限**
3. 若订阅链接不慎泄露，无需重置管理密钥，直接在后台删除对应订阅即可阻断访问
4. 管理面板仅支持 `/ADMIN_TOKEN` 路径访问，URL 参数 `?token=` 无法绕过后台鉴权限制

## 🎨 页面交互特色
1. 新生成订阅自动置顶在订阅列表最上方
2. 订阅区域紧凑轻量化布局：链接输入框与删除按钮同行，不再单独展示原始随机 Token
3. 点击订阅链接输入框一键复制剪贴板，弹出简洁成功提示
4. 所有删除操作附带确认弹窗，防止误操作销毁分组/订阅

## 💾 数据持久化说明
分组配置、订阅绑定关系全部持久化存储在绑定的 Workers KV：
1. 新增/修改/删除分组、订阅操作实时写入 KV
2. 更换 Worker 实例时，仅需绑定**同一个 KV 命名空间**即可无缝复用全部历史数据
3. Cloudflare KV 无自动备份机制，关键节点信息建议自行本地留存备份

## ❓ 常见问题 FAQ
### Q：直接访问根域名返回 403
A：根路径不携带合法订阅 Token 参数，会直接拒绝访问；订阅访问必须带上 `?token=xxx`；管理后台需访问 `/ADMIN_TOKEN` 专属路径

### Q：访问管理路径提示权限不足
A：
1. 路径格式错误：域名后直接拼接 `/ADMIN_TOKEN`，不能使用 `?token=` 参数形式
2. 检查环境变量 `ADMIN_TOKEN` 是否填写正确，修改环境变量后必须重新部署 Worker

### Q：生成的订阅链接无法访问
A：确认该订阅未在后台被删除；无额外日志推送渠道，可重新生成订阅测试

### Q：订阅中的节点名称为何多了 `[分组名]-` 前缀
A：为了在客户端中更直观地区分节点归属，Worker 分组时，会在订阅下发时为每个分组内的节点名称附加 `[分组名]-` 前缀；该前缀仅出现在订阅响应中，不会修改你在管理面板 textarea 中所粘贴/编辑的原始节点文本；如果你修改了分组名，下次拉取订阅时会自动使用新前缀；如需完全自定义名称，可直接在节点文本中自行加上前缀并保持 `[分组名]-` 格式，避免重复添加

### Q：删除分组后相关订阅失效
A：设计逻辑：分组删除同步清理所有绑定该分组的订阅；如需保留节点，先编辑分组清空节点而非直接删除分组
# Cloudflare Workers 多分组订阅服务
## 项目简介
基于 Cloudflare Workers + KV 存储构建的节点订阅管理工具，权限严格隔离：
- 管理后台：依靠环境变量 `ADMIN_TOKEN` 路径鉴权
- 订阅访问：独立 24 位随机 Token，订阅凭证**不能进入管理面板**
### 核心能力
1. 多分组管理：创建/编辑/删除节点分组，独立维护各组节点列表
2. 自由组合订阅：勾选任意数量分组，生成专属订阅链接
3. 自定义订阅名：可选填写订阅名称，留空自动拼接分组名作为展示名
4. 订阅权限管控：后台一键删除订阅，对应链接立刻 403 失效
5. 外部订阅合并：支持导入多条外部订阅，与本地节点聚合输出
6. 订阅标准输出：聚合节点后 Base64 编码返回标准订阅响应

## 部署前置条件
1. 拥有 Cloudflare 账号，可使用 Workers & Pages
2. 新建一个 Workers KV 命名空间（持久化分组、订阅绑定数据）

## 环境变量配置清单
| 变量名 | 必填 | 说明 |
|--------|------|------|
| `ADMIN_TOKEN` | ✅ | 高强度随机字符串，管理后台访问密钥，禁止泄露 |
| `SUBAPI` | ❌ | 外部订阅地址，多地址使用 `|` 分隔，自动合并入订阅 |
| `SUBNAME` | ❌ | 页面自定义标题 |
| `SUBUPDATE_TIME` | ❌ | 响应头 Profile-Update-Interval（单位秒），默认 6 |
| `URL302` | ❌ | 非法访问302跳转地址 |
| `URL` | ❌ | 反向代理地址 |

## 完整部署步骤
1. 进入 Cloudflare 控制台 → Workers & Pages → KV，创建 KV 命名空间，记录名称
2. Workers & Pages → 创建应用程序 → 创建 Worker，自定义名称后部署
3. 快速编辑 Worker，清空默认代码，粘贴项目完整 JS 代码并保存
4. 切换至「设置」→「变量」，找到 KV 命名空间绑定：
   - 绑定变量名固定为：`KV_STORE_KEY`
   - 选择上一步创建的 KV 命名空间，保存
5. 同页面「环境变量」区域，添加表格内变量，**必须配置 ADMIN_TOKEN**
6. 返回代码编辑器，点击「保存并部署」使所有配置生效

## 使用指南
### 1. 进入管理后台
访问地址格式：`https://你的worker域名/你的ADMIN_TOKEN`
示例：ADMIN_TOKEN = `RandomStrongPass123` → `xxx.workers.dev/RandomStrongPass123`

#### 管理后台三大区域
##### ① 节点分组管理
- 新增分组：点击「新增分组」填写分组名称
- 编辑分组：点击「编辑」修改分组名称与节点，绿色保存按钮提交
- 删除分组：点击删除，**所有绑定该分组的订阅会同步清除**

##### ② 勾选分组生成订阅
1. 可选输入框自定义订阅名称，留空自动生成展示名称
2. 勾选一个/多个分组复选框
3. 点击「生成订阅链接」，新订阅置顶展示在下方列表

##### ③ 已创建订阅列表（直接位于生成区域下方）
- 展示订阅名称、绑定分组清单
- 点击订阅链接输入框：自动复制完整订阅链接
- 右侧删除按钮：销毁订阅，对应访问立即返回 403

### 2. 使用订阅链接
订阅地址格式：`https://你的worker域名?token=24位随机订阅Token`
- 无 Token / 无效 / 已删除 Token：403 Access Denied
- 有效 Token：聚合绑定分组节点 + 外部订阅，输出 Base64 订阅

## 安全重要提醒
1. `ADMIN_TOKEN` 使用复杂随机字符串，切勿简单数字/词汇，不要对外分享
2. 订阅独立 Token 仅可获取节点，**无法访问管理面板**
3. 订阅泄露无需重置管理密钥，直接在后台删除对应订阅即可
4. 管理面板仅支持 `/ADMIN_TOKEN` 路径访问，`?token=` 参数不能绕过后台鉴权

## 页面交互特性
1. 新生成订阅自动置顶在列表顶部
2. 订阅区域紧凑布局：链接输入框与删除按钮同行，不单独展示原始 Token
3. 点击订阅输入框自动复制内容，弹窗提示复制成功
4. 删除操作带确认弹窗，防止误删分组/订阅

## 数据持久化说明
分组、订阅绑定数据全部存储在绑定的 Workers KV：
1. 增删改分组/订阅实时写入 KV
2. 更换 Worker 仅需绑定同一 KV 命名空间即可复用数据
3. KV 无自动备份，关键节点建议自行留存备份

## 常见问题解答
### Q: 直接访问根域名返回 403
A: 根路径缺少合法订阅 Token 参数；订阅必须携带 `?token=xxx`；管理后台需访问 `/ADMIN_TOKEN` 路径

### Q: 访问管理路径提示权限不足
A: 1. 路径格式错误：域名后直接拼接 `/ADMIN_TOKEN`，不要使用 `?token=`；2. 检查环境变量 ADMIN_TOKEN，修改后需重新部署 Worker

### Q: 生成的订阅链接无法访问
A: 确认未删除该订阅

### Q: 删除分组后相关订阅失效
A: 设计逻辑：分组删除同步清理所有绑定该分组的订阅；如需保留节点，先编辑分组清空节点而非直接删除分组